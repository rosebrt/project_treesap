function line(_speaker, _color, _text, _responses) constructor {
	speaker = _speaker;
	color = _color;
	text = _text;
	responses = _responses;
}

function response(_text, _next_line, _action) constructor {
	text = _text;
	next_line = _line;
	action = _action;
}